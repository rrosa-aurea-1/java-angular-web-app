{
	"name": "cordova-app",
	"description": "Full cordova app with backend",
	"version": "1.0.0",
	"main": "main.js",
	"author": {
		"name": "Angelo Manganiello",
		"email": "angelo.mang@libero.it"
	},
	"license": "Apache-2.0",
	"scripts": {
		"preconfig": "cpx  \"../frontend-app/dist/**/*.*\"  dist -C -p  && cd dist && cd .. ",
		"start": "npm run preconfig && cordova run android"
	},
	"devDependencies": {
		"cpx": "1.5.0"
	},
	"dependencies": {
	}
}
